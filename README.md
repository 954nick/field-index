# Field Index v0.7.0 - Visible Player Rating Regression Hotfix

This patch changes non-Head in-game regression saves to validate ratings that CFB27 displays directly (preferably Speed/SPD) instead of raw OverallRating. It also updates BATCH tests to use directly visible ratings.

Extract the contents directly into the Field Index project root and replace existing files.
